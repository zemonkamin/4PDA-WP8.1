<Page
    x:Class="_4PDA.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_4PDA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#FF050505">

    <Page.Resources>
        <Style x:Key="NoTiltListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ChatImageTemplate">
            <Border
                Margin="0,8,0,0"
                Background="#FF101010"
                BorderBrush="#FF242424"
                BorderThickness="1">
                <Image
                    Source="{Binding Image}"
                    Stretch="Uniform"
                    MaxHeight="260"
                    HorizontalAlignment="Left" />
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="MessageTemplate">
            <StackPanel Margin="0,0,0,10">
                <Grid
                    Margin="0,9,0,10"
                    Visibility="{Binding DateSeparatorVisibility}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Grid.Column="0"
                        Height="1"
                        Background="#FF454545"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0" />

                    <TextBlock
                        Grid.Column="1"
                        Text="{Binding DaySeparatorText}"
                        FontSize="12"
                        Foreground="#FF8A8A8A"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />

                    <Border
                        Grid.Column="2"
                        Height="1"
                        Background="#FF454545"
                        VerticalAlignment="Center"
                        Margin="8,0,0,0" />
                </Grid>

                <StackPanel Margin="18,0,18,0" MaxWidth="980" HorizontalAlignment="Stretch">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Text="{Binding Author}"
                            Foreground="{Binding AuthorBrush}"
                            FontSize="18"
                            FontWeight="SemiBold"
                            TextWrapping="NoWrap"
                            TextTrimming="WordEllipsis" />

                        <TextBlock
                            Grid.Column="1"
                            Text="{Binding TimeText}"
                            Visibility="{Binding TimeVisibility}"
                            Foreground="#FF7D7D7D"
                            FontSize="12"
                            VerticalAlignment="Bottom"
                            Margin="10,0,0,2" />
                    </Grid>

                    <TextBlock
                        Text="{Binding Text}"
                        Foreground="#FFCFCFCF"
                        FontSize="17"
                        TextWrapping="Wrap"
                        Margin="0,1,0,0" />

                    <ItemsControl
                        ItemsSource="{Binding Images}"
                        ItemTemplate="{StaticResource ChatImageTemplate}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="62" Background="#FF111111" Padding="12,6,16,6" BorderBrush="#22FFFFFF" BorderThickness="0,0,0,1">
            <Grid MaxWidth="980" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Content="←"
                    Width="46"
                    MinHeight="42"
                    Margin="0,0,12,0"
                    Click="BackButton_Click" />

                <TextBlock
                    x:Name="DialogTitleTextBlock"
                    Grid.Column="1"
                    FontSize="24"
                    VerticalAlignment="Center"
                    TextWrapping="NoWrap"
                    TextTrimming="WordEllipsis"
                    MaxLines="1" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Background="#FF050505">
            <ListView
                x:Name="MessagesListView"
                ItemTemplate="{StaticResource MessageTemplate}"
                ItemContainerStyle="{StaticResource NoTiltListViewItemStyle}"
                SelectionMode="None"
                Background="Transparent"
                BorderThickness="0" />

            <ProgressRing
                x:Name="ChatProgressRing"
                Width="60"
                Height="60"
                IsActive="False"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </Grid>

        <TextBlock
            x:Name="ChatStatusTextBlock"
            Grid.Row="2"
            FontSize="14"
            Opacity="0.72"
            TextWrapping="Wrap"
            Margin="12,4,12,4" />

        <Border Grid.Row="3" Background="#FF101010" Padding="12" BorderBrush="#22FFFFFF" BorderThickness="0,1,0,0">
            <Grid MaxWidth="980" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    x:Name="MessageTextBox"
                    Grid.Column="0"
                    MinHeight="44"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    PlaceholderText="сообщение" />

                <Button
                    x:Name="SendButton"
                    Grid.Column="1"
                    Content="отправить"
                    Margin="8,0,0,0"
                    Click="SendButton_Click" />
            </Grid>
        </Border>
    </Grid>
</Page>
